import en from "./json/en.json" with { type: "json" };

// Overrides for the dashboard's built-in English strings, plus any custom
// keys for widgets and routes. Keys here take precedence over the defaults.
export default {
  en: {
    translation: en,
  },
};
